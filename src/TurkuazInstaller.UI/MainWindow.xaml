<!--
# 📄 Dosya Yolu: /src/TurkuazInstaller.UI/MainWindow.xaml
# 📌 Amac: TurkuazInstaller foundation UI shell ekranini tanimlar
# 📌 Modul - View XAML
# Version: 0.1.0
# Aciklama: Kullanici metinlerini ViewModel binding uzerinden gosteren ilk WinUI 3 ekrandir

Bagimli Oldugu Katman: View
-->
<Window
    x:Class="TurkuazInstaller.UI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="32">
        <StackPanel
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Spacing="12"
            MaxWidth="560">
            <TextBlock
                Text="{x:Bind ViewModel.Title, Mode=OneWay}"
                FontSize="32"
                FontWeight="SemiBold" />
            <TextBlock
                Text="{x:Bind ViewModel.Subtitle, Mode=OneWay}"
                TextWrapping="Wrap"
                Opacity="0.72" />
            <InfoBar
                IsOpen="True"
                IsClosable="False"
                Severity="Informational"
                Title="{x:Bind ViewModel.StatusTitle, Mode=OneWay}"
                Message="{x:Bind ViewModel.StatusMessage, Mode=OneWay}" />
        </StackPanel>
    </Grid>
</Window>
